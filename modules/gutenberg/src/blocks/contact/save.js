/**
 * WordPress dependencies
 */
import { Fragment } from '@wordpress/element';

const save = ( props ) => {
	const {
		clientId,
		attributes,
		className,
		isSelected,
		setAttributes
	} = props;

	const {
		text,
	} = attributes

	return (
		<Fragment>
			<div></div>	
		</Fragment>
	);
};

export default save;
